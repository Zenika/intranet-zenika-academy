import React from 'react';
import '../layout/Layout.scss';

const ProgramData = () => (
  <section className="section box mbmd}">
    <h1 className="title is-4 is-spaced">Programme 1 - Renforcement JS</h1>
  </section>
);

export default ProgramData;
